trigger:
- main
- feature/*

pr:
- main
- feature/*

resources:
- repo: self

variables:
  vmImageName: 'ubuntu-latest'

stages:
- stage: SonarCloudAnalysis
  displayName: SonarCloud Analysis Stage

  jobs:
  - job: Analyze
    displayName: Run SonarCloud Analysis

    pool:
      vmImage: $(vmImageName)

    steps:
    - checkout: self
      fetchDepth: 0

    # NOSONAR - Azure DevOps task references support only the major version (@3);
    # a full semver (@3.x.y) is not valid syntax, so the version is left as-is.
    - task: SonarCloudPrepare@3 # NOSONAR
      displayName: 'Prepare SonarCloud Analysis'
      inputs:
        SonarCloud: 'sdk_sonar'
        organization: 'fastpix'
        scannerMode: 'CLI'
        configMode: 'manual'
        cliProjectKey: 'FastPix_ruby-language-sdk'
        cliProjectName: 'ruby-language-sdk'
        extraProperties: |
          sonar.projectKey=FastPix_ruby-language-sdk
          sonar.organization=fastpix
          sonar.projectName=ruby-language-sdk
          sonar.sources=.
          sonar.exclusions=vendor/**
          sonar.sourceEncoding=UTF-8

    - script: |
        echo "Running SonarCloud analysis for Ruby SDK"
      displayName: 'Build Step'

    - task: SonarCloudAnalyze@3 # NOSONAR
      displayName: 'Run SonarCloud Code Analysis'

    - task: SonarCloudPublish@3 # NOSONAR
      displayName: 'Publish SonarCloud Quality Gate Results'
      inputs:
        pollingTimeoutSec: '300'